import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function LoteHistorial() {
  return (
    <div className="space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>Historial del Lote</CardTitle>
        </CardHeader>
        <CardContent>
          Contenido próximamente.
        </CardContent>
      </Card>
    </div>
  );
}
