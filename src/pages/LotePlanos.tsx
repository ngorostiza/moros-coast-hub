import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function LotePlanos() {
  return (
    <div className="space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>Planos y Documentos</CardTitle>
        </CardHeader>
        <CardContent>
          Contenido próximamente.
        </CardContent>
      </Card>
    </div>
  );
}
